Act as a Senior Web3 Full-Stack Developer and Blockchain Architect specializing in Stellar Soroban (Rust) and Next.js. I want to build a decentralized liquidity protocol on the Stellar network inspired by Meteora on Solana, featuring a Dynamic Liquidity Market Maker (DLMM).
Generate a production-ready, highly secure full-stack boilerplate including the on-chain smart contracts, backend integration indexing, and frontend UI.
### 1. TECHNICAL STACK REQUIREMENTS
- Smart Contracts: Stellar Soroban SDK, Rust, WebAssembly (Wasm).
- Frontend: Next.js 14+ (App Router), TypeScript, TailwindCSS, shadcn/ui.
- Stellar Integration: @stellar/stellar-sdk, @stellar/stellar-wallets-kit (for Freighter/Albedo support).
- State/Indexing: Mercury indexing or a lightweight custom event listener solution.
### 2. CORE ARCHITECTURE & FEATURES TO IMPLEMENT
#### A. On-Chain Smart Contracts (Soroban / Rust)
Create a modular Soroban contract architecture containing:
1. Dynamic Liquidity Market Maker (DLMM):
   - Implement liquidity bins (discrete price points instead of a continuous XYK curve).
   - Functions for `add_liquidity_bin`, `remove_liquidity_bin`, and `swap_exact_in_bin`.
   - Implement dynamic fee pricing logic that increases fees during high volatility based on time-elapsed between trades.
2. Dynamic Vaults:
   - A vault contract that accepts asset deposits and can reallocate unutilized capital to yield-bearing integrations (e.g., blend protocol or simple lending pools if available, stub this out if needed).
3. Math Library:
   - Safe math implementation for bin price calculations ($P = 1.0001^{\text{bin\_id}}$) optimized for Soroban's `i128` types.
#### B. Frontend UI/UX (Next.js & Tailwind)
Create a modern, dark-themed DeFi dashboard containing:
1. Swap Interface:
   - Token selector, price impact estimator, routing indicator, and slippage tolerance settings.
2. Liquidity Provision (LP) Dashboard:
   - Visual distribution picker for DLMM bins (Spot, Curve, Bid-Ask shapes).
   - "Deposit" and "Withdraw" modals handling custom bin ranges.
3. Wallet Connection:
   - Stellar Wallets Kit implementation displaying user balances (Native XLM and custom SAC tokens).
### 3. OUTPUT EXPECTED
Please provide the implementation step-by-step with clean, well-commented code blocks:
1. The `Cargo.toml` and complete Rust code for the DLMM Soroban contract (`lib.rs`).
2. The TypeScript functions for encoding/decoding Soroban XDR types for `swap` and `add_liquidity`.
3. The Next.js React component for the advanced DLMM Liquidity Distribution Selector.
Focus heavily on memory optimization and precise arithmetic to avoid Soroban CPU/Memory limit exhaustion.